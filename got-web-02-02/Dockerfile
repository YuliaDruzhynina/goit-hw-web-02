# Використовуємо офіційний образ Python з версією 3.8
FROM python:3.8

# Встановлюємо змінну середовища для робочої директорії
ENV APP_HOME /app

# Створюємо робочу директорію всередині контейнера
WORKDIR $APP_HOME

# Копіюємо файли застосунку в робочу директорію контейнера
COPY . .

# Встановлюємо залежності Python з файлу requirements.txt
RUN pip install -r requirements.txt

# Вказуємо порт, на якому буде працювати наш застосунок
EXPOSE 5000

# Вказуємо команду, яка буде виконуватися при запуску контейнера
CMD ["python", "hw.py"]
